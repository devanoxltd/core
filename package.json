{
    "name": "devanoxltd-core",
    "private": true,
    "description": "Developer setup and browser test tooling for the Core package.",
    "type": "module",
    "scripts": {
        "postinstall": "playwright install chromium",
        "setup": "composer install && npm install",
        "test:browser": "composer test:browser"
    },
    "devDependencies": {
        "playwright": "^1.61.1"
    }
}
